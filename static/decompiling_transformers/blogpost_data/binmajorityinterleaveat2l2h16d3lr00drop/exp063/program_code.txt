1. s1 = select(q=pos, k=pos, op=(k%3==q%3==0))	# layer 0 head 0
2. a1 = aggregate(s=s1, v=token) 	  # layer 0 head 0
3. new_a1 = element_wise_op(a1) 	  # layer 1 mlp
4. logits1 = project(inp=new_a1, op=(inp==out))
5. logits2 = project(op=\circled{b})
6. prediction = softmax(logits1+
			logits2)