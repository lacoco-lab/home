1. a1 = aggregate(s=[], v=token) 	  # layer 0 head 0
2. new_token = element_wise_op(token) 	  # layer 1 mlp
3. new_a1 = element_wise_op(a1) 	  # layer 1 mlp
4. logits1 = project(inp=new_token, op=(inp==out))
5. logits2 = project(inp=new_a1, op=(inp==out))
6. logits3 = project(op=\circled{a})
7. prediction = softmax(logits1+
			logits2+
			logits3)