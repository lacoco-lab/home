1. a1 = aggregate(s=[], v=token) 	  # layer 0 head 0
2. new_token = element_wise_op(token) 	  # layer 0 mlp
3. logits1 = project(inp=a1, op=\circled{a})
4. logits2 = project(inp=new_token, op=(inp==out))
5. logits3 = project(op=\circled{b})
6. prediction = sigmoid(logits1+
			logits2+
			logits3)