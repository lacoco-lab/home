1. s1 = select(k=token, op=(k==BOS))
2. a1 = aggregate(s=s1, v=pos) 	  # layer 0 head 1
3. m1 = mlp(pos+a1) 	  # layer 0 mlp
4. logits1 = project(inp=m1, op=(inp==out))
5. prediction = sigmoid(logits1)