1. a1 = aggregate(s=[], v=token) 	  # layer 0 head 2
2. new_token = element_wise_op(token) 	  # layer 0 mlp
3. new_a1 = element_wise_op(a1) 	  # layer 0 mlp
4. new_new_token = element_wise_op(new_token) 	  # layer 1 mlp
5. logits1 = project(inp=new_token, op=(inp==out))
6. logits2 = project(inp=new_a1, op=(inp==out))
7. logits3 = project(inp=new_new_token, op=(inp==out))
8. prediction = softmax(logits1+
			logits2+
			logits3)