1. s1 = select(q=token, k=token, op=\circled{a})	# layer 0 head 0
2. a1 = aggregate(s=s1, v=token) 	  # layer 0 head 0
3. logits1 = project(inp=token, op=\circled{b})
4. logits2 = project(inp=a1, op=\circled{c})
5. prediction = softmax(logits1+
			logits2)