1. s1 = select(q=pos, k=pos, op=\circled{a})	# layer 0 head 0
2. a1 = aggregate(s=s1, v=token) 	  # layer 0 head 0
3. s2 = select(q=pos, k=pos, op=(k==q-1))	# layer 0 head 2
4. a2 = aggregate(s=s2, v=token) 	  # layer 0 head 2
5. m1 = mlp(token+a1+a2) 	  # layer 0 mlp
6. s3 = select(q=m1, k=m1, op=(q==k))	# layer 1 head 2
7. a3 = aggregate(s=s3, v=m1) 	  # layer 1 head 2
8. logits1 = project(inp=a3, op=(inp==out))
9. prediction = softmax(logits1)