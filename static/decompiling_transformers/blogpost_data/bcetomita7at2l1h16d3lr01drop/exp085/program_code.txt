1. s1 = select(q=token, k=token, op=\circled{a})	# layer 0 head 0
2. a1 = aggregate(s=s1, v=token) 	  # layer 0 head 0
3. s2 = select(q=token, k=a1, op=(k==q),
		special_op=(uniform selection))	# layer 1 head 0
4. a2 = aggregate(s=s2, v=a1) 	  # layer 1 head 0
5. logits1 = project(inp=a2, op=\circled{c})
6. logits2 = project(op=\circled{d})
7. prediction = sigmoid(logits1+
			logits2)