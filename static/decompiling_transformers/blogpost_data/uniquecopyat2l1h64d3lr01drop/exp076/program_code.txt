1. s1 = select(q=pos, k=pos, op=(k==q-1))	# layer 0 head 0
2. a1 = aggregate(s=s1, v=token) 	  # layer 0 head 0
3. new_token = element_wise_op(token) 	  # layer 0 mlp
4. s2 = select(q=new_token, k=a1, op=(q==k))	# layer 1 head 0
5. a2 = aggregate(s=s2, v=token) 	  # layer 1 head 0
6. logits1 = project(inp=a2, op=\circled{b})
7. prediction = softmax(logits1)