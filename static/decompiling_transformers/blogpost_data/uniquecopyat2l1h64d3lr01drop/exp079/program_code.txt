1. s1 = select(q=pos, k=pos, op=(k==q-1))	# layer 0 head 0
2. a1 = aggregate(s=s1, v=token) 	  # layer 0 head 0
3. sharpened_token = sharpen(token) 	  # layer 0 mlp
4. s2 = select(q=sharpened_token, k=a1, op=(k==q),
		special_op=(k==BOS))	# layer 1 head 0
5. a2 = aggregate(s=s2, v=token) 	  # layer 1 head 0
6. logits1 = project(inp=a2, op=(inp==out),
		special_op=(uniform selection))
7. logits2 = project(op=\circled{d})
8. prediction = softmax(logits1+
			logits2)