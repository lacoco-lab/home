1. s1 = select(q=pos, k=pos, op=(k==q-1))	# layer 0 head 0
2. a1 = aggregate(s=s1, v=token) 	  # layer 0 head 0
3. s2 = select(q=token, k=a1, op=(k==q),
		special_op=(k==BOS))	# layer 1 head 0
4. a2 = aggregate(s=s2, v=token) 	  # layer 1 head 0
5. logits1 = project(inp=a2, op=(inp==out),
		special_op=(uniform selection))
6. logits2 = project(op=\circled{d})
7. prediction = softmax(logits1+
			logits2)