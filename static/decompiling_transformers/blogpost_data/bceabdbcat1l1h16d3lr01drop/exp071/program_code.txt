1. s1 = select(k=token, op=\circled{a})
2. a1 = aggregate(s=s1, v=token) 	  # layer 0 head 0
3. logits1 = project(inp=a1, op=\circled{b})
4. logits2 = project(op=\circled{c})
5. prediction = sigmoid(logits1+
			logits2)