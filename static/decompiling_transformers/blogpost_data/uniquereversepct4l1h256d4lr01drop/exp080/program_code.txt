1. s1 = select(q=pos, k=pos, op=\circled{a})	# layer 0 head 0
2. s2 = select(k=token, op=\circled{b})
3. a1 = aggregate(s=s1+s2, v=token) 	  # layer 0 head 0
4. s3 = select(q=token, k=token, op=(k==q),
		special_op=(uniform selection))	# layer 2 head 0
5. s4 = select(q=token, k=a1, op=\circled{d})	# layer 2 head 0
6. a2 = aggregate(s=s3+s4, v=a1) 	  # layer 2 head 0
7. new_a2 = element_wise_op(a2) 	  # layer 3 mlp
8. logits1 = project(inp=new_a2, op=(inp==out))
9. prediction = softmax(logits1)